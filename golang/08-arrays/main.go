package main

// Arrays in Go are fixed-size sequences of elements of the same type. They are useful for storing a collection of data when the size is known at compile time.

func main() {
	var arr [5]int
	arr[0] = 1
	arr[1] = 2
	arr[2] = 3
	arr[3] = 4
	arr[4] = 5
}	